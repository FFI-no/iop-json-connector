{
  "title": "CreateCommandEvent",
  "messageId": "01f6",
  "isCommand": true,
  "description": "This message is used to set up a command event.   Field 1 is a local request ID that       the event provider returns in the Confirm or Reject message.  Field 2 is the maximum       allowed execution time; any command not completed within its specified duration is        considered a failure.  Field 3 contains the size of the Command message that is to        specify the command to be executed.  Field 4 contains the encoded command message         (including its two byte header).",
  "type": "object",
  "properties": {
    "HeaderRec": {
      "type": "object",
      "comment": "",
      "required": [
        "MessageID"
      ],
      "properties": {
        "MessageID": {
          "type": "string",
          "jausType": "unsigned short integer",
          "comment": "A two byte field to hold the message ID of a message",
          "const": "01f6"
        }
      }
    },
    "CreateEventRec": {
      "type": "object",
      "comment": "",
      "required": [
        "RequestID",
        "MaximumAllowedDuration",
        "CommandMessage"
      ],
      "properties": {
        "RequestID": {
          "type": "number",
          "jausType": "unsigned byte",
          "comment": "Local request ID for use in confirm event"
        },
        "MaximumAllowedDuration": {
          "type": "number",
          "jausType": "unsigned integer",
          "comment": "Any commands not executed within the maximum allowed time are considered a failure."
        },
        "CommandMessage": {
          "type": "object",
          "comment": "The JAUS Command message to be used by the receiving component to generate the command event.",
          "required": [
            "payloadMessageId",
            "payload"
          ],
          "properties": {
            "payloadMessageId": {
              "type": "string",
              "jausType": "unsigned short integer",
              "comment": "message id of the payload message"
            },
            "payload": {
              "type": "object",
              "comment": "The JAUS Command message to be used by the receiving component to generate the command event.",
              "required": [],
              "properties": {}
            }
          },
          "minLength": 2,
          "jausType": "unsigned integer",
          "fieldFormat": "JAUS MESSAGE"
        }
      }
    }
  },
  "required": [
    "HeaderRec",
    "CreateEventRec"
  ]
}