{
  "title": "RemoveNoGoZone",
  "messageId": "d741",
  "isCommand": true,
  "description": "This message is used to remove the specified no-go zone from the cost map.  Once removed, the cells shall return to their normal values, or to an uncertain (zero confidence) state.",
  "type": "object",
  "properties": {
    "DefaultHeaderRec": {
      "type": "object",
      "comment": "",
      "required": [
        "MessageID"
      ],
      "properties": {
        "MessageID": {
          "type": "string",
          "jausType": "unsigned short integer",
          "comment": "",
          "const": "d741"
        }
      }
    },
    "ZoneIDRec": {
      "type": "object",
      "comment": "",
      "required": [
        "ZoneID"
      ],
      "properties": {
        "ZoneID": {
          "type": "number",
          "jausType": "unsigned short integer",
          "comment": "ID of the zone to be reported. If the specified value is zero (0), all zones will be reported."
        }
      }
    }
  },
  "required": [
    "DefaultHeaderRec",
    "ZoneIDRec"
  ]
}