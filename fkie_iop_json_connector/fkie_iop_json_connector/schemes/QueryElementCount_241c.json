{
  "title": "QueryElementCount",
  "messageId": "241c",
  "isCommand": false,
  "description": "This message is used to query the number of elements in a list.",
  "type": "object",
  "properties": {
    "HeaderRec": {
      "type": "object",
      "comment": "",
      "required": [
        "MessageID"
      ],
      "properties": {
        "MessageID": {
          "type": "string",
          "jausType": "unsigned short integer",
          "comment": "A two byte field to hold the message ID of a message",
          "const": "241c"
        }
      }
    }
  },
  "required": [
    "HeaderRec"
  ]
}